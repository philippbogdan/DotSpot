// Blindsighted App Configuration
// Copy this file to Config.xcconfig and fill in your values
// Config.xcconfig is gitignored and should never be committed

// API Backend Configuration
API_BASE_URL = https:/$()/api.blindsighted.hails.info

// LiveKit Server Configuration
LIVEKIT_SERVER_URL = wss:/$()/blindsighted-iogq73td.livekit.cloud
LIVEKIT_AGENT_NAME = example_agent

// Development Configuration (optional - for hardcoded token testing)
// Leave empty to generate tokens dynamically via API
LIVEKIT_DEV_TOKEN =
LIVEKIT_DEV_ROOM_NAME =
